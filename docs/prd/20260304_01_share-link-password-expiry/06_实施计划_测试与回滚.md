# 06 实施计划、测试与回滚

**成文日期**：2026-03-04 22:50:29 UTC+8
**最后修订**：2026-03-04 22:51:34 UTC+8

本文档用于定义实施阶段、测试矩阵与回滚方案。阅读时当前系统实现可能已发生变化，请以实际代码与产品行为为准，谨慎参考。

---

## 实施阶段

### 阶段 A：后端与数据层

1. 增加 `shares` 新字段与迁移。
2. 扩展 DTO 与 repo 字段映射。
3. 新增 `verify-access`、`regenerate-protected` 接口。
4. 统一公开读取接口门禁，强制 `shareId` 校验（受限模式）。
5. 增加错误码与审计事件。

### 阶段 B：前端交互与路由

1. 分享弹窗新增“受限分享”配置项（密码 + 分钟）。
2. 公开页新增密码输入门禁与错误态页面。
3. 受限模式访问请求注入内存态访问令牌。
4. 旧路由 `/share/p/:pageSlug` 的受限访问直接拦截。

### 阶段 C：联调与灰度

1. 开启受限模式灰度开关（workspace 维度）。
2. 小流量验证创建、访问、过期、重建主链路。
3. 扩大灰度并观察错误码与失败率。

## 测试矩阵

| 用例ID | 类别 | 前置条件 | 操作 | 期望 |
| --- | --- | --- | --- | --- |
| ACC-001 | 参数校验 | 登录且有分享权限 | `expiresInMinutes=31` 创建 | 400 + `SHARE_TTL_INVALID` |
| ACC-002 | 正向功能 | 创建受限分享 | 返回链接 | 包含 `share.key` 与 `expiresAt` |
| ACC-003 | 鉴权成功 | 未过期 + 正确密码 | 访问公开页 | 可读取页面内容 |
| ACC-004 | 鉴权失败 | 未过期 + 错误密码 | 访问公开页 | 401 + `SHARE_PASSWORD_INVALID` |
| ACC-005 | 过期控制 | 已过期 | 访问公开页 | 410 + `SHARE_EXPIRED` |
| ACC-006 | 重建策略 | 已过期受限分享 | 调用重建接口 | 返回新 key，旧 key 失效 |
| ACC-007 | 继承策略 | includeSubPages=true | 访问子页 | 受同一密码与过期时间控制 |
| ACC-008 | 绕过防护 | 仅 pageSlug 无 shareId | 访问受限内容 | 无法访问 |
| ACC-009 | 搜索防护 | 受限分享未验证 | 调用 share-search | 401 `SHARE_PASSWORD_REQUIRED` |
| ACC-010 | 审计完整性 | 连续密码失败 | 查看日志 | 有失败事件与来源信息 |

## 回归范围

1. 普通公开分享链路（不应回归破坏）。
2. 分享列表、分享开关、子页面分享继承。
3. 分享页树加载、分享内搜索、SEO 渲染。
4. 工作区/空间级“禁用公开分享”开关行为。

## 监控与发布门槛

| 指标 | 门槛 | 触发动作 |
| --- | --- | --- |
| `verify-access` 5xx 比例 | >1% 持续 5 分钟 | 暂停灰度 |
| `SHARE_PASSWORD_INVALID` 峰值 | 超历史 3 倍 | 启动风控排查 |
| 受限分享访问 P95 | >800ms 持续 10 分钟 | 性能降级或回滚 |

## 回滚策略

### 回滚锚点

1. 锚点 1：后端 feature flag 关闭受限模式入口。
2. 锚点 2：前端隐藏受限模式 UI，仅保留普通公开分享。
3. 锚点 3：门禁逻辑回退到旧公开读取策略（临时）。

### 回滚步骤

1. 先关前端入口，防止新增受限分享继续写入。
2. 再回退后端门禁逻辑，恢复公开访问可用性。
3. 保留数据库新增字段，不做紧急删字段操作。
4. 故障恢复后再评估二次发布。

## 风险与缓解

| 风险 | 描述 | 缓解措施 |
| --- | --- | --- |
| UX 过严导致投诉 | 不记住密码会增加输入成本 | 明确提示“刷新后需重新输入” |
| 旧链接不可用 | 无 `shareId` 路由被限制 | 给出迁移公告和重建入口 |
| 实现遗漏链路 | 某接口未接入门禁 | 联调阶段执行全链路清单验收 |

## 修改日志

- 2026-03-04 22:50:29 UTC+8：初始化文档骨架。
- 2026-03-04 22:51:34 UTC+8：补充实施阶段、测试矩阵、监控门槛与回滚步骤。
